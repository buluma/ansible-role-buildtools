---
- name: Verify
  hosts: all
  become: false
  gather_facts: false

  tasks:
    - name: run gcc
      ansible.builtin.command:
      changed_when: false
        cmd: gcc --version

    - name: run make
      ansible.builtin.command:
      changed_when: false
        cmd: make --version

    - name: run shasum
      ansible.builtin.command:
      changed_when: false
        cmd: shasum --version

    - name: run bison
      ansible.builtin.command:
      changed_when: false
        cmd: bison --version
